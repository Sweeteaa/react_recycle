import React from 'react';
import LogisticsMsg from '../LogisticsMsg';

const Commit = () => {
    return (
        <div>
            <LogisticsMsg/>
            待评论
        </div>
    );
};

export default Commit;